# UI Mockup Notes

ภาพ .png ในโฟลเดอร์นี้คือ mockup จริงที่ต้อง implement ตาม
(ออกแบบด้วย Claude Design)

โฟลเดอร์ source/ เป็นไฟล์ prototype ต้นฉบับ — ใช้เป็นข้อมูลอ้างอิง
เรื่อง copy ข้อความ/สี/layout เท่านั้น **ไม่ใช่โค้ดให้ก็อปปี้ใช้ตรง ๆ**
เพราะใช้ runtime เฉพาะของเครื่องมือออกแบบ (x-dc), ไม่ compatible
กับ Next.js